/**
 * Created by zhanghongdou on 16/9/18.
 */
function insertSort(arr) {
    var len = arr.length;
    var preIndex,current;
    for(var i = 1; i < len; i++) {
        preIndex = j - 1;
        current = arr[i];
        while(preIndex >= 0 && arr[preIndex] > current) {
            arr[preIndex = ]
        }
    }
}